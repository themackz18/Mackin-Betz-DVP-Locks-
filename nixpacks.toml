[phases.setup]
cmds = [
  "pip install --no-cache-dir -r requirements.txt",
  "playwright install --with-deps chromium"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-8080} --workers 1 --threads 2"
